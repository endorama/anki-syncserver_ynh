## Important Notes

- This sync server does **not** support LDAP or SSO authentication
- Users must be manually configured through the YunoHost config panel
- A default user with **an insecure password** is automatically created, you **must** change it
- Up to 5 users can be configured
- Each user needs credentials in the format: `username:password`
- If installing in a subpath, remember to include the trailing slash when configuring your Anki clients!

